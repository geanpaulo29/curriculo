function scrollToAbility(className) {
    var targetElement = document.querySelector("." + className);

    if (targetElement) {
        targetElement.scrollIntoView({
            behavior: 'smooth'
        });
    }
}

function scrollToObj(elementId) {
    var targetElement = document.getElementById(elementId);

    if (targetElement) {
        targetElement.scrollIntoView({
            behavior: 'smooth'
        });
    }
}

function scrollToAcademic(elementId) {
    var targetElement = document.getElementById(elementId);

    if (targetElement) {
        targetElement.scrollIntoView({
            behavior: 'smooth'
        });
    }
}

function scrollToProfss(elementId) {
    var targetElement = document.getElementById(elementId);

    if (targetElement) {
        targetElement.scrollIntoView({
            behavior: 'smooth'
        });
    }
}